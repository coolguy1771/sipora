# Changelog

Per-release notes are maintained in the repository file **`CHANGELOG.md`** at the workspace root so version bumps stay next to `Cargo.toml` and release automation. This documentation site does not duplicate that file, to avoid drift.

Before tagging a release, follow [releasing](/RELEASING) and move items from **Unreleased** into the new version section in `CHANGELOG.md`.
